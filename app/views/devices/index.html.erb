<h1>Devices</h1>

<ul>
<%- @devices.each do |device| %>
  <li>
    <%=link_to( device.description, device_path(device.uid) )%>
    (last seen <%=human_timestamp(device.last_heartbeat)%>)
  </li>
<% end %>
</ul>
